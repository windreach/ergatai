[package]
name = "simple-agent"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"
publish = false

[dependencies]
# MCP client SDK (connects to Ergatai, receives notifications)
rmcp = { version = "3.1.2", features = [
    "client",
    "transport-streamable-http-client-reqwest",
] }
tokio = { version = "1.36", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
anyhow = "1.0"
