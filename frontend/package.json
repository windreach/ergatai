{
  "name": "@ergatai/frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "overrides": {
    "monaco-editor": "^0.53.0",
    "dompurify": "^3.2.4"
  },
  "scripts": {
    "dev": "npm run dev --workspace @ergatai/web",
    "build": "npm run build --workspace @ergatai/web",
    "preview": "npm run preview --workspace @ergatai/web",
    "lint": "oxlint"
  },
  "devDependencies": {
    "oxlint": "^1.79.0",
    "typescript": "~6.0.2"
  }
}
