import { ToolWorkspace } from "./ToolWorkspace";

export function WorkspaceShell() {
  return (
    <div className="h-full min-h-0 w-full overflow-hidden bg-bg text-text">
      <ToolWorkspace />
    </div>
  );
}
