[package]
name = "ergatai-agent"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"
authors = ["Ergatai Team"]
license = "Apache-2.0"
repository = "https://github.com/windreach/Ergatai"
description = "Agent utilities for Ergatai (middleware mode - no hosting)"
publish = false  # Placeholder crate, not yet ready for publishing

[dependencies]
# Placeholder — no dependencies needed in middleware mode
